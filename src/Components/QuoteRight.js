import styled from 'styled-components'

const QuoteRight = styled.i`
  position: absolute;
  right: 25px;
  top: 40px;
  font-size: 25px;
  color: rgba(255, 255, 255, 0.3);

  @media (max-width: 768px) {
    font-size: 18px;
    right: 10px;
    top: 30px;
  }
`

export default QuoteRight
